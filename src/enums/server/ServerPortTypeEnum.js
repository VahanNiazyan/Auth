const ServerPortTypeEnum = Object.freeze({
    TYPE_PUB_PRIMARY: 'public-primary',
    TYPE_PUB_SECONDARY: 'public-secondary',
    TYPE_SVC_DHCP: 'svc-dhcp',
    TYPE_PRIVATE: 'private'
})

export default ServerPortTypeEnum